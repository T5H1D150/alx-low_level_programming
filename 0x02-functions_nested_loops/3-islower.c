#include "main.h"

/**
 * _islower - Is character lower or upper case
 * @c: accepts character
 * Return: 1 if c is lower case, else returns 0
 *
 */

int _islower(int c)
{
	char i;

	c = 1;

	for (i = 'a' ; i <= 'z' ; i++)
	{
	return (c);
	}
	return (c);
}
