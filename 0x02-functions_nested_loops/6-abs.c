#include "main.h"
/**
 * _abs - Function for abs value of integer
 * @x: accepts integer value
 * Return: Abs value
 *
 */

int _abs(int x)
{
	int y = -1;

	if (x >= 0)
		return (x);
	else
		return (x * y);
}
