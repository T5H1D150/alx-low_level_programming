#include <stdio.h>
#include <string.h>
/**
 * int _strlen - eturns string length
 * @s: pointer to string
 * Return: String lenght
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);

	return (len);
}
