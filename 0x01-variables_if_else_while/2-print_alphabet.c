#include <stdio.h>

/**
 * main - entry point
 *
 * Return: 0 Always
 *
 */

int main(void)
{
	char Alpha;

	for (Alpha = 'a' ; Alpha <= 'z' ; Alpha++)
	{
	putchar(Alpha);
	}
	putchar('\n');
	return (0);
}
